@{
  ViewBag.Title = "Email Confirmed";
}
<div class="container mt-5">
  <div class="alert alert-success text-center shadow p-4 rounded-3">
    <h1 class="mb-4"><i class="bi bi-check-circle-fill"></i> Email Confirmed!</h1>
    <p class="lead">Your email has been confirmed successfully 🎉</p>
    <a class="btn btn-success mt-3" href="@Url.Action("Login", "Account")">Login Now</a>
  </div>
</div>
